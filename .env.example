# DeepSeek API Key（必填）
DEEPSEEK_API_KEY=sk-xxx

# eNSP 所在主机：本机直连默认 127.0.0.1；Docker Desktop 部署用 host.docker.internal
ENSP_HOST=127.0.0.1

# MySQL 密码（Docker compose 默认 12345678，本机按实际填写）
MYSQL_ROOT_PASSWORD=12345678
